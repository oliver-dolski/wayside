* text=auto
*.pcap binary
*.pcapng binary
*.ambr text eol=lf
*.ps1 text eol=crlf
*.py text eol=lf

# The example report artifacts (REPORT-05) are compared byte for byte
# against freshly generated files (tests/test_example_report.py) - line
# ending normalization at checkout would then put a file that went through
# it side by side with one that did not (the same failure mode for which the
# determinism gate from Phase 2 refuses to compare against a file from the
# repository - tests/test_determinism.py).
examples/4sics/report.md -text
examples/4sics/analysis.json -text
examples/4sics/report.pdf binary
