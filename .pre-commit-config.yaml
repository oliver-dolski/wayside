repos:
  - repo: local
    hooks:
      - id: confidentiality-guard
        name: Confidentiality gate (normative text / standards/.local)
        entry: python scripts/confidentiality_guard.py
        language: python
        pass_filenames: true
        stages: [pre-commit]
