# Identyfikator z pola `id` jest tym samym lancuchem, ktory trafia do pola
# `protocol` kazdego zdarzenia i do etykiety wiersza macierzy komunikacji
# (PROTO-05). Ta sama wartosc, ktora dataclassa `LowConfidenceEvent` juz
# niesie w polu `protocol`.
id: modbus-rtu-over-tcp
confidence: low
dissector: "modbus_rtu_tunnel_dissector:dissect"
